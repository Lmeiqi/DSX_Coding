# Question 5 - Clinical Data API (FastAPI)

## Run locally

1. Ensure `adae.csv` exists in this folder.
2. Install dependencies:

```bash
pip install fastapi uvicorn pandas
```

3. Start API:

```bash
uvicorn main:app --reload
```

4. Open docs:
- http://127.0.0.1:8000/docs
